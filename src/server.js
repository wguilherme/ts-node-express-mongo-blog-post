const app = require('./app')

const PORT = 3000

app.listen(PORT, () => console.log("Server is running at PORT 3000 🚀"))


// server application

// app.get('/api', (req, res) => {
//   res.send('API online')
// })